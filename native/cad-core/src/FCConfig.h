#pragma once

// Minimal standalone shim for FreeCAD's FCConfig.h.
// Upstream carries build-config macros; PlaneGCS needs none of them.
// This empty header satisfies the include without pulling FreeCAD's build.
