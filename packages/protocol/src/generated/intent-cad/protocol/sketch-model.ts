// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { SketchArc } from '../../intent-cad/protocol/sketch-arc.js';
import { SketchCircle } from '../../intent-cad/protocol/sketch-circle.js';
import { SketchConstraint } from '../../intent-cad/protocol/sketch-constraint.js';
import { SketchLine } from '../../intent-cad/protocol/sketch-line.js';
import { SketchPoint } from '../../intent-cad/protocol/sketch-point.js';


export class SketchModel {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SketchModel {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSketchModel(bb:flatbuffers.ByteBuffer, obj?:SketchModel):SketchModel {
  return (obj || new SketchModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSketchModel(bb:flatbuffers.ByteBuffer, obj?:SketchModel):SketchModel {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SketchModel()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

points(index: number, obj?:SketchPoint):SketchPoint|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new SketchPoint()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

pointsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

lines(index: number, obj?:SketchLine):SketchLine|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new SketchLine()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

linesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

circles(index: number, obj?:SketchCircle):SketchCircle|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new SketchCircle()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

circlesLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

arcs(index: number, obj?:SketchArc):SketchArc|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new SketchArc()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

arcsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

constraints(index: number, obj?:SketchConstraint):SketchConstraint|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new SketchConstraint()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

constraintsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startSketchModel(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addPoints(builder:flatbuffers.Builder, pointsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, pointsOffset, 0);
}

static createPointsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startPointsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addLines(builder:flatbuffers.Builder, linesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, linesOffset, 0);
}

static createLinesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startLinesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addCircles(builder:flatbuffers.Builder, circlesOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, circlesOffset, 0);
}

static createCirclesVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startCirclesVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addArcs(builder:flatbuffers.Builder, arcsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, arcsOffset, 0);
}

static createArcsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startArcsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addConstraints(builder:flatbuffers.Builder, constraintsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, constraintsOffset, 0);
}

static createConstraintsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startConstraintsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endSketchModel(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSketchModel(builder:flatbuffers.Builder, pointsOffset:flatbuffers.Offset, linesOffset:flatbuffers.Offset, circlesOffset:flatbuffers.Offset, arcsOffset:flatbuffers.Offset, constraintsOffset:flatbuffers.Offset):flatbuffers.Offset {
  SketchModel.startSketchModel(builder);
  SketchModel.addPoints(builder, pointsOffset);
  SketchModel.addLines(builder, linesOffset);
  SketchModel.addCircles(builder, circlesOffset);
  SketchModel.addArcs(builder, arcsOffset);
  SketchModel.addConstraints(builder, constraintsOffset);
  return SketchModel.endSketchModel(builder);
}
}
